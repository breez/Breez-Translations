name: breez_translations
description: A new Flutter package project.
version: 1.0.0

environment:
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=3.7.12"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: '>=0.17.0 <0.20.0'

dev_dependencies:
  build_runner: ">=2.0.0 <2.5.0"
  flutter_test:
    sdk: flutter
  flutter_lints: ">=2.0.2 <3.1.0"

dependency_overrides:
  intl: '>=0.17.0 <0.20.0'

flutter:
  generate: true
